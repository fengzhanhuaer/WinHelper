#pragma once

// ---------------------------------------------------------------------------
// Matrix digital-rain background  (one independent drop per grid column)
// Call RenderMatrixBackground() once per frame after ImGui::NewFrame().
// ---------------------------------------------------------------------------

void RenderMatrixBackground();
